{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */.claude/agents/*.md) cd /Users/nsingh/Documents/AI-Agents-Skills && ./scripts/setup-agents.sh ;; esac; } 2>/dev/null || true",
            "statusMessage": "Syncing agents globally..."
          }
        ]
      }
    ]
  }
}
